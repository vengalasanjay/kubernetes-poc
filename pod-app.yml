apiVersion: v1
kind: Pod
metadata:
  name: vproapp
  labels:
    app: vproapp
spec:
  containers:
  - name: vproapp
    image: sanjayvengala16/myapp:30
    ports:
    - name: vproapp-port
      containerPort: 8080
